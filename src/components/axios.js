import axios from 'axios';

// export default ({ requiresAuth = false, baseURL = "" } = {}) => {
//     if (!baseURL) {
//         baseURL = process.env.MIX_CLEANNING_API_URL;
//     }

//     const instance = axios.create({ baseURL: baseURL });
//     // if (requiresAuth) {
//     //     instance.defaults.headers.common['Authorization'] = 'Bearer ' + localStorage.getItem('client_credentials');
//     // }
    
//     return instance;
// };

